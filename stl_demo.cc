#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <string>

int main()
{

  // Vector + Sort
  std::vector<int> x {4, 0, 1, 2, 3};
  y = x;



  // Map
  std::map<std::string, int> m;
  m["Newton"] = 1643;
  m["Faraday"] = 1791;
  m["Curie"] = 1867;
  m["Rutherford"] = 1871;

  // Now how do we iterate through these entries?


  

  return 0;
}
